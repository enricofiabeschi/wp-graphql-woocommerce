name: Release Version

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release tag (e.g. v1.0.0)"
        required: true
      ref:
        description: "Git ref to release from (branch, tag, or commit SHA). Default: dist"
        required: false
        default: "dist"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0

      - name: Validate version tag format
        run: |
          set -e
          TAG="${{ inputs.version }}"
          echo "$TAG" | grep -Eq '^v[0-9]+(\.[0-9]+){1,2}([+.-][0-9A-Za-z.-]+)?$' \
            || (echo "Invalid tag: $TAG. Examples: v1.0.0, v2.6.0-lava1" && exit 1)

      - name: Fail if tag already exists (remote)
        run: |
          set -e
          TAG="${{ inputs.version }}"
          git fetch --tags
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag already exists: $TAG"
            exit 1
          fi

      - name: Setup PHP + Composer
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.0"
          tools: composer:v2
          extensions: mbstring, intl
          coverage: none

      - name: Install dependencies (no-dev)
        run: |
          composer install --no-dev --optimize-autoloader --no-interaction --no-progress

      - name: Build ZIP artifact (composer archive)
        run: |
          set -e
          rm -rf plugin-build
          mkdir -p plugin-build
          composer archive -vvv --format=zip --file="plugin-build/wp-graphql-woocommerce"
          ls -lah plugin-build

      - name: Create git tag and push
        run: |
          set -e
          TAG="${{ inputs.version }}"
          git tag "$TAG"
          git push origin "$TAG"

      - name: Create GitHub Release + upload zip
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          name: ${{ inputs.version }}
          files: plugin-build/wp-graphql-woocommerce.zip
